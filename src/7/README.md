# Git Log
> show commit log 

## Command

- Log
> also you can add `--oneline` to view only one line or `--graph` to show branching
```sh
git log 
```

- Detail Log
> show detail log
```sh
git show FIRST_7_HASH
```
